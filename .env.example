## Copy this file to .env.local and fill in real values. DO NOT commit secrets.
## For local development, you can keep .env.local uncommitted and use this file as a template.

# Public URL your app will be served from (used in redirects)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Supabase (public) URL and anon key (public key is safe-ish for frontend but keep secrets out of repo)
NEXT_PUBLIC_SUPABASE_URL=https://<PROJECT_REF>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase service role key (server-side only!) - never commit this. Keep in your deployment secrets.
SUPABASE_SERVICE_ROLE_KEY=

# Strava OAuth app credentials (server-side secret)
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=

# Optional Vercel OIDC token for automated deployments / CLI use (server-side secret)
VERCEL_OIDC_TOKEN=
